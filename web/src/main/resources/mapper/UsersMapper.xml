<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myq.mapper.UsersMapper">
    <resultMap id="BaseResultMao" type="com.myq.model.Users">
        <id column="id" javaType="INTEGER" property="id" />
        <result column="name" javaType="VARCHAR" property="name" />
        <result column="account" javaType="VARCHAR" property="account" />
        <result column="password" javaType="VARCHAR" property="password" />
        <result column="sex" javaType="INTEGER" property="sex" />
        <result column="info" javaType="VARCHAR" property="info" />
    </resultMap>

    <insert id="insert" parameterType="com.myq.model.Users">
        insert into `user`
        (`name`, account, `password`, sex, info)
        values
        (#{name,javaType="VARCHAR"}, #{account,javaType="VARCHAR"},#{password,javaType="VARCHAR"},
        #{sex,javaType="INTEGER"}),#{info,javaType="VARCHAR"}
    </insert>

    <select id="selectAccount" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select id, `name`, account, password, sex, info
        from `user`
        where account = #{account, javaType="VARCHAR"}
    </select>

    <select id="seleteAll" resultMap="BaseResultMap">
        select id, `name`, account, password, sex, info
        from `user`
    </select>
</mapper>
